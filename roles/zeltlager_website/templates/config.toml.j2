max_members = 100
max_members_reached = '<div class="alert alert-primary" role="alert">Das Zeltlager für dieses Jahr ist leider schon voll.</div>'
error_message = "{{ zeltlager_error_message }}"
bind_address = "127.0.0.1:{{ zeltlager_website_port }}"
test_mail = "{{ test_mail }}"
domain = "{{ mail_domain }}"

sentry = "{{ sentry_dsn }}"

[sender_mail]
name = "Die Zeltlager Betreuer"
address = "anmeldung@{{ mail_domain }}"

[sender_mail_account]
host = "{{ mail_hostname }}"
name = "hostmaster@{{ mail_domain }}"
password = "{{ zeltlager_website_mail_password }}"

[[additional_mail_receivers]]
name = "Ansgar"
address = "{{ zeltlager_website_additional_mail_receiver }}"

#[discourse]
#endpoint = "https://discourse.meinzeltlager.com"
#token = "{{ discourse_token }}"
#username = "{{ discourse_user }}"
#group = "{{ discourse_group }}"
#category = "{{ discourse_category }}"
